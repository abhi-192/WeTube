extends layouts/main
include mixins/videoBlock

block content
    .videosArr
        each item in videosArr
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })
            